
<div class=" panel panel-default">
  <div class="panel-body">

<p style="color: green"><%= notice %></p>

<style> 
  th,h2,p,h1{ color:black; }
  td{ color:black; }
</style>

<div id="movies">
  <!DOCTYPE html>
<html lang="en">
<head>
  <title>Bootstrap Example</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>

<div class="container mt-3">
  <h2>Bordered Table</h2>
  <p>The .table-bordered class adds borders on all sides of the table and the cells:</p>            
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>id</th>
        <th>Title</th>
        <th>Description</th>
        <th>Movie length</th>
        <th>Director</th>
        <th>Ratting</th>
        <th>Show Movies </th>
        <th>Edit Movies </th>
        <th>update Movies </th>


      </tr>
    </thead>
  <% @movies.each do |movie| %>
  <tbody>      
    <tr style="color:black;">
      <td><%= movie.id %></td>
      <td><%= movie.title %></td>
      <td><%= movie.description %></td>
      <td><%= movie.movie_length %></td>
      <td><%= movie.director %></td>
      <td><%= movie.ratting %></td>
      <td><button type="button" class="btn btn-outline-secondary"><%= link_to "Show this movie", movie %></button>
      </td>
      <td><button type="button" class="btn btn-outline-secondary"><%= link_to "Edit", movie %></button>
      </td>
      <td><button type="button" class="btn btn-outline-secondary"><%= link_to "Destroy", @movie, method: :delete %></button>
      </td>
    </tr>
  </tbody>
    <% end %>
</table>
</div>
</body>
</html>
</div>
</div>
</div>

<button type="button" class="btn btn-outline-secondary"><%= link_to "New movie", new_movie_path %></button>

<!-- <div class="row">
  < @movies.each do |movie| %>
  <div class="col-sm-6 col-md-3">
  <div class="thumbnail">
    < link_to (image_tag movie.img.url(:medium), class:'image', movie) %>
  </div>
</div> -->
<!-- 
they both are same 
<div class="row">
      < @movies.each do |movie| %>
      <div class="col-sm-6 col-md-3">
        <div class="thumbnail">
        < link_to (image_tag @movies.image.url(:medium), class:'image', movie) %>

        </div>
      </div> -->